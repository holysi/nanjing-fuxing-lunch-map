# 開發規劃

## 第一階段：靜態午餐地圖（已完成）

- GitHub Pages 可直接託管 HTML、CSS、JavaScript 與 JSON。
- 地圖與列表共用同一份店家資料，切換檢視不會改變篩選條件。
- 類別、價格區間、平日／假日午餐可組合篩選。
- 店家詳情包含名稱、營業時段、簡述、類別、價格、照片欄位與步行導航。
- 不確定的欄位顯示「待確認」；照片缺席時顯示料理示意圖。
- 管理者直接編輯 `data/places.json` 並推送到 `main` 即可更新公開資訊。

上線前需補齊精確座標、確認各店營業時間與實際菜單價格，並加入獲授權的店家或餐點照片。首批資料適合功能展示與小範圍試用，尚未完成逐店實地查核。

## 第二階段：LINE 登入、評論與投稿

GitHub Pages 繼續負責公開網站；另建一個受控的後端 API。LINE 登入的密鑰只存放在後端，前端不保存長期憑證。後端需要資料庫保存店家、評論、投稿與審核紀錄，照片放在有權限控制的物件儲存空間。

1. **登入**：使用者透過 LINE 登入後，後端建立站內使用者識別；管理者角色由後端權限設定決定。
2. **評論**：已登入者可對店家發表文字與上傳照片。後端限制檔案類型、大小與發文頻率，並提供檢舉或移除流程。
3. **新增地點**：已登入者填寫名稱、地址、類別、價格、營業時間、簡述與照片。新資料先進入 `pending` 狀態，公開地圖只讀取 `published` 資料。
4. **管理審核**：管理者檢視投稿、修正資料、核准或退回；保留提交者與審核時間。審核通過才公開。
5. **資料遷移**：將現有 JSON 作為初始店家資料匯入後端；前端的顯示元件與篩選邏輯可沿用，資料來源再改為公開 API。

## 第二階段完成標準

- 未登入者能瀏覽所有已發布店家，但不能送出評論或投稿。
- 一般使用者無法直接刊登或修改已發布店家。
- 管理者核准前，新地點不出現在公開地圖與列表。
- 照片上傳失敗或審核退回時，使用者看到明確狀態，不會誤以為已發布。
- API 驗證身分與權限；前端顯示狀態不作為安全依據。
